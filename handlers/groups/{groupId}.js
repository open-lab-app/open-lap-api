'use strict';
/**
 * Operations on /groups/{groupId}
 */
module.exports = {
    /**
     * Returns a group with the specified id

     * parameters: groupId
     * produces: application/json
     */
    get: function getGroupById(req, res) {
        res.send(501);
    },
    /**
     * Updates a group given an id

     * parameters: groupId, body
     * produces: application/json
     */
    put: function updateGroupById(req, res) {
        res.send(501);
    }
};