'use strict';
/**
 * Operations on /groups/{groupId}/users
 */
module.exports = {
    /**
     * By passing a group id, returns all users
that are members of a group

     * parameters: groupId
     * produces:
     */
    get: function getUsersByGroup(req, res) {
        res.send(501);
    }
};