'use strict';
/**
 * Operations on /groups/{groupId}/invitations
 */
module.exports = {
    /**
     * This endpoint allows users with admin rights to a group to
to invite users to openLab

     * parameters: groupId, body
     * produces:
     */
    post: function (req, res) {
        res.send(501);
    }
};