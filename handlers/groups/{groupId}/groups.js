'use strict';
/**
 * Operations on /groups/{groupId}/groups
 */
module.exports = {
    /**
     * Gets all children of the specified
group up to a maximum level of recursion.

     * parameters: groupId, level
     * produces: 
     */
    get: function getChildrenForGroup(req, res) {
        res.send(501);
    }
};