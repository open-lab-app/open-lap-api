'use strict';
/**
 * Operations on /groups/{groupId}/posts/findByAuthor
 */
module.exports = {
    /**
     * Finds posts for an author

     * parameters: groupId, authorId
     * produces: application/json
     */
    get: function findPostsByAuthorId(req, res) {
        res.send(501);
    }
};