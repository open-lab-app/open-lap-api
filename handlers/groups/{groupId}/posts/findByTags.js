'use strict';
/**
 * Operations on /groups/{groupId}/posts/findByTags
 */
module.exports = {
    /**
     * Multiple tags can be passed with comma-separated strings

     * parameters: groupId, tags
     * produces: application/json
     */
    get: function findPostsByTags(req, res) {
        res.send(501);
    }
};