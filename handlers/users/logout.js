'use strict';
/**
 * Operations on /users/logout
 */
module.exports = {
    /**
     *
     * parameters:
     * produces: application/json
     */
    get: function logoutUser(req, res) {
        res.send(501);
    }
};