'use strict';
/**
 * Operations on /users/login
 */
module.exports = {
    /**
     * This endpoint allows a user to login onto the system

     * parameters: username, password
     * produces: application/json
     */
    post: function loginUser(req, res) {
        res.send(501);
    }
};