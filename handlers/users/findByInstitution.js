'use strict';
/**
 * Operations on /users/findByInstitution
 */
module.exports = {
    /**
     * lists all users that belong to a given institution
     * parameters: institution
     * produces: 
     */
    get: function getUsersByInstitution(req, res) {
        res.send(501);
    }
};