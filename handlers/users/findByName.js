'use strict';
/**
 * Operations on /users/findByName
 */
module.exports = {
    /**
     * search for a user by name
     * parameters: name
     * produces:
     */
    get: function getUsersByName(req, res) {
        res.send(501);
    }
};